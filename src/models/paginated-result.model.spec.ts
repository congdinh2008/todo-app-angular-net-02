import { PaginatedResultModel } from './paginated-result.model';

describe('PaginatedResultModel', () => {
  it('should create an instance', () => {
    expect(new PaginatedResultModel()).toBeTruthy();
  });
});
